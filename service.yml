apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: blue-green
  labels:
    app: nginx
    k8s: blue-green
spec:
  type: ClusterIP
  ports:
  - port: 80
    name: http
  selector:
    app: nginx